# Home Assistant Notes

## SSH access

Use the built-in SSH port (22222) to reach the host OS:

```bash
ssh root@homeassistant.local -p 22222
```
## Heat pump
The heat pump is connected via USB to the home assistant rpi